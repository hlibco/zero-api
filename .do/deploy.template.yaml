# https://www.digitalocean.com/docs/app-platform/references/app-specification-reference/
spec:
  name: api-zero
  services:
    - build_command: yarn build
      environment_slug: node-js
      git:
        branch: main
        repo_clone_url: https://github.com/hlibco/api-zero.git
      envs:
        - key: PLANET
          value: 'Earth'
      name: api-zero
